#include "main.h"
/**
 * _abs - program that computes the absolute value of an integer
 * @n: an int to be check
 * Return: value of an int
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
